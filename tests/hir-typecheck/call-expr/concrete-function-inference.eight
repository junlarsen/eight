// RUN: not %eightc --emit-hir --terminator=mir --emit-query hir.fn.test %s 2>&1 | FileCheck %s

intrinsic_fn zero() -> i32;

fn test() -> i32 {
// CHECK-LABEL: test
  let k = zero();
// CHECK-NEXT: let k: i32
  return k;
}

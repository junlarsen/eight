// RUN: not %eightc --emit-hir --emit-query hir.fn.test_i32_unaryop --terminator=hir %s 2>&1 | FileCheck %s

trait Neg<A, R> { fn neg(a: A) -> R; }
instance Neg<i32, i32> { intrinsic_fn neg(a: i32) -> i32; }

fn test_i32_unaryop() {
// CHECK-LABEL: fn test_i32_unaryop
  let unaryop_i32_neg = -1;

// CHECK-NEXT: let unaryop_i32_neg: i32 = [[EXPAND:\(+]]@@builtin_i32_neg
}

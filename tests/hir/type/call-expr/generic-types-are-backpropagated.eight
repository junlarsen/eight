// RUN: not %eightc %s --emit-hir --emit-query hir.fn.main --terminator=mir 2>&1 | FileCheck %s

intrinsic_fn id<T>(x: T) -> T;

fn main() -> i32 {
// CHECK-LABEL: main
  let k = id(1);
// CHECK-NEXT: let k: i32 = ((id::<i32>
  return 0;
}

// RUN: %not %eightc %s --syntax-only 2>&1 | %fc %s

intrinsic_fn id<T>(x: T) -> T;

fn main() -> i32 {
  let k = id::<i32, bool>(123);
  return 0;
}

// CHECK-LABEL: sema::wrong_function_type_argument_count

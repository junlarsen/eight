// RUN: not %eightc %s 2>&1 | FileCheck %s

trait Neg<A, R> {
  fn neg(a: A) -> R;
}

intrinsic_fn garbage(v: i32) -> i32;

fn test(a: i32) -> i32 {
  let k = -garbage;
  return 0;
}

// CHECK-LABEL: sema::trait_missing_instance
// CHECK: trait Neg does not have instance Neg<fn(i32) -> i32, _>

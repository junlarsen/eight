// RUN: not %eightc %s 2>&1 | FileCheck %s

fn test(in: i32) {
  let infer = *in;
}

// It seems like we're not able to detect that this is an error.

// CHECK-LABEL: sema::dereference_of_non_pointer
// CHECK: i32 is not dereferenceable

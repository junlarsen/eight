// RUN: not %eightc %s 2>&1 | FileCheck %s

intrinsic_fn malloc<T>(size: i32) -> *T;

fn test() -> i32 {
  let infer: i32 = *malloc;
  return infer;
}

// TODO: This error message should be better

// CHECK-LABEL: sema::dereference_of_non_pointer
// CHECK: fn(i32) -> *_ is not dereferenceable
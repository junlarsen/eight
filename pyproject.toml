[project]
name = "xd"
version = "0.1.0"
description = "XD programming language compiler"
authors = [
    {name = "Mats Jun Larsen",email = "mats@jun.codes"}
]
license = {text = "Apache 2.0"}
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "lit (>=18.1.8,<19.0.0)"
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[package]
name = "eight-span"
version = "0.1.0"
edition = "2021"

[features]
default = ["serde"]
serde = ["dep:serde"]

[dependencies]
miette = { workspace = true }
serde = { workspace = true, optional = true }
